---
dependencies:
- role: certificates
  vars:
    cert_data: "{{ envoy_cert }}"
    cert_name: "{{ envoy_cert_name }}"
    key_data: "{{ envoy_key }}"
    key_name: "{{ envoy_key_name }}"
    ca_cert_data: "{{ envoy_ca_cert }}"
    ca_cert_name: "{{ envoy_ca_cert_name }}"
