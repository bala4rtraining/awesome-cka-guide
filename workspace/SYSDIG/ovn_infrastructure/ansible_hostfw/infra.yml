---
- include: "../ovn_vars.yml"

# Use this playbook to actually create or modify the shape and type of the machines the rest of the ansible project requires.
- hosts: localhost
  gather_facts: yes
  tasks:
  - name: need to identify list of inventory describing the different types of servers and roles
    debug: msg="This is purely for posterity and flair."
